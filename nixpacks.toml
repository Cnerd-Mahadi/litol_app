[phases.setup]
nixPkgs = ['php81Extensions.grpc']

[phases.install]
cmds = ["mkdir -p /var/log/nginx && mkdir -p /var/cache/nginx", "php --ini", "composer install", "npm i", "cp .env.example .env", "php artisan optimize:clear"]
